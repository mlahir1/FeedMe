<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hackumbc.fedme.fedme">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher12"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher12"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:screenOrientation="portrait">

        <activity
            android:name=".SplashScreenActivity">
            <intent-filter>

                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".FilterListActivity" />
        <activity android:name=".PlacesActivity" />
        <activity android:name=".FreeFoodActivity" />
        <activity android:name=".UserRatingActivity" />
        <activity android:name=".PostFreeFood" />
        <activity android:name=".MainActivity"></activity>
    </application>

</manifest>